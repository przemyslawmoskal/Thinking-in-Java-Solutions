Write a method that takes two String arguments
uses all the boolean comparisons to compare the two Stings and print the results.
For the == and !=, also perform the equals() test.
In main(), test your method with some different String objects.

Chapter 03 Exercise 14