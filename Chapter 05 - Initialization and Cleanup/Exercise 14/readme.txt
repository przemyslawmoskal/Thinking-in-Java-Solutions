Create class with static String field initialized at point of definition
and another one initialized by the static block.  Add static method that prints
both fields and demonstrates that they are both initialized before they are used.

Chapter 05 Exercise 14