In the innerclasses/GreenhouseController.java example, GreenhouseController contains
a hard-coded set of events. Change the program so that it reads the events
from a text file. Version 11b: Use a Factory Method design pattern to build
the events - see Thinking in Patterns (with Java) at www.mindview.net.

Chapter 18 Exercise 11